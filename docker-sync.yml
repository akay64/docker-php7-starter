version: "2"

options:
  verbose: true
syncs:
  ${SYNC_CONTAINER_NAME}-sync: # tip: add -sync and you keep consistent names as a convention
    src: './app'
    sync_strategy: 'native_osx'
    sync_excludes: ['ignored_folder', '.ignored_dot_folder']
